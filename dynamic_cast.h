#ifndef DYNAMIC_CAST_H
#define DYNAMIC_CAST_H


class dynamic_cast
{
    public:
        dynamic_cast();
        virtual ~dynamic_cast();

    protected:

    private:
};

class derived : public dynamic_cast
{
    public:
        derived();
        virtual ~derived();

    protected:

    private:
};

#endif // DYNAMIC_CAST_H
